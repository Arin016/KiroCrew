# Last-synced upstream MeshClaw tips (one SHA per tracked branch).
# Updated at the END of every sync run (Step 1/7). The meshclaw-sync skill bounds
# the candidate set by these SHAs — see skills/meshclaw-sync/SKILL.md Step 1.
# Format: "<branch> <sha>  # <date> <note>"
beta e62422ae      # 2026-06-09 boundary advanced past a pure-SKIP run (batch-9). No ports.
# Batch-9 (cron-fired): sole candidate e62422ae "fix(writing-review): send Midway cookie on
# scanner sync fetch" = SKIP / NA_INTERNAL. Adversarially verified: whole commit serves the
# ABSENT writing_review subsystem + STUBBED midway. No portable hunk — the new
# hooks.safe_read_file_internal allowlist's only entry is scanner_sync.midway_cookie (dead
# consumer), and re-adding ".midway" to security._SENSITIVE_HOME_DIRS is explicitly forbidden
# by MIGRATION_PLAN.md L145/L382-384 + CLAUDE.md (de-Amazon decision). Boundary advances past
# it (SKIP is resolved, unlike a DEFERRED commit). No b490c7e8-style hold needed — backlog clear.
mainline 49633d05  # 2026-06-09 no mainline-only delta past this
