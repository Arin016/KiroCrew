# Last-synced upstream MeshClaw tips (one SHA per tracked branch).
# Updated at the END of every sync run (Step 7). The meshclaw-sync skill bounds
# the candidate set by these SHAs — see skills/meshclaw-sync/SKILL.md Step 1.
# Format: "<branch> <sha>  # <date> <note>"
beta 3a5af268      # 2026-06-09 boundary advanced to beta tip; backlog fully cleared.
# Batch-8 ports (this run): b490c7e8 dynamic subagent auto-sizing (previously DEFERRED, now
# ported — relocated the absent mcp_gateway.pool /proc helpers into subagent.py), 0632630e
# doctor live-MCP-probe + redacted stderr drain, 3a5af268 slack subagent-completion persist.
# 11973f4c (approval_policy on mode toggle) was already ported in batch-7. No DEFERRED items remain.
mainline 49633d05  # 2026-06-09 no mainline-only delta past this
